import { motion } from "framer-motion";

export default function Home({ onEnter }) {
  return (
    <main className="min-h-screen bg-gradient-to-b from-amber-50 to-white flex flex-col items-center justify-center text-center font-serif text-brown-800 relative overflow-hidden">
      {/* Fundo em aquarela */}
      <div className="absolute inset-0 opacity-20 bg-[url('https://www.transparenttextures.com/patterns/paper-fibers.png')] bg-repeat"></div>

      {/* Frase com brilho */}
      <motion.h1
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 2 }}
        className="text-3xl md:text-4xl font-bold text-pink-500 drop-shadow-md"
      >
        💍 Bem-vindo à nossa história de amor 💞
      </motion.h1>

      {/* Botão com brilho suave */}
      <motion.button
        onClick={onEnter}
        whileHover={{ scale: 1.05 }}
        animate={{ scale: [1, 1.03, 1], transition: { repeat: Infinity, duration: 2 } }}
        className="mt-8 px-8 py-3 bg-pink-400 text-white font-semibold rounded-full shadow-lg hover:bg-pink-500 focus:outline-none"
      >
        Entrar
      </motion.button>
    </main>
  );
}
